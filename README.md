# -random-bg-generator
 A responsive Random Color Generator built with HTML, CSS, and JavaScript. Hit the button and watch the container’s background switch to a random color from a preset array, all thanks to a bit of Math.random() and direct DOM updates.
